{
  "name": "recap",
  "version": "1.0.3",
  "description": "official website for the reCAP (un)conference",
  "main": "index.html",
  "scripts": {
    "start": "ui5 serve --accept-remote-connections --open 'index.html'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ../docs/*; ui5 build --dest ../docs --exclude-task=createDebugFiles"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ui5/cli": "^3",
    "ui5-middleware-livereload": "latest"
  }
}
